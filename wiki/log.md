# Operation Log

Append-only. Format: `## [YYYY-MM-DD] operation | description`

## [2026-06-11] ingest | Initial research: inventoried both Jekyll blogs (~145 paired posts each, YAT theme, active), found martingamsby.com WordPress dead (HTTP 500), listed public GitHub repos, read CV identity, confirmed Music-Experiment-Game repo is private, linktrees block scrapers.

## [2026-06-11] ingest | Plan decided with Martin: one unified Astro site in this repo, doors-as-lenses UX (Software/Physics/Book/Music/Everything), blogs migrate in, /fr + /en mirrored routes. Written to PLAN.md.

## [2026-06-11] ingest | Martin confirmed: employer work demos (Lattice HF Space) go on the dev door with attribution; repo gets LLM wiki + skills + CLAUDE.md. Wiki seeded.

## [2026-06-11] ingest | Resolved open questions 1+2: linktree links extracted from Martin's saved pages (revealed per-language X, Bluesky, Facebook FR, Typeshare EN accounts); physics preprint is Zenodo 10.5281/zenodo.20482196 (paper+code record, v1.0.2, 2026-05-31). New page [[physics-preprint]]; rewrote [[profiles-and-channels]]. Email: martin.gamsby@ is official but NOT to be published without explicit OK (new open question #6).

## [2026-06-12] ingest | Phase 1 scaffold built and verified (Astro 5.18, 20 pages, twin links + base path + root redirect checked in dist). Discovery: Jekyll posts carry a `ref:` frontmatter field naming their twin URL — primary pairing signal for Phase 2 migration; bodies duplicate the title as a `### **…**` heading (drop at migration). Updated [[jekyll-blogs]] and PLAN.md.

## [2026-06-12] lint | Repo made PUBLIC (free-plan GitHub Pages requires it; Martin chose scrub-and-publish). Emails and personal details scrubbed from wiki + PLAN.md; history squashed to a single clean root commit so the scrubbed strings never existed in public history. Standing rule: no email addresses or private personal details anywhere in this repo.

## [2026-06-12] ingest | SITE IS LIVE at https://martingamsby.github.io/martingamsby.com/ — deploy workflow green, FR homepage verified (doors, posts, footer socials). Gotcha: enabling Pages via API on a private free-plan repo fails 422; flipping the repo public auto-enabled Pages in `legacy` mode, switched to `workflow` via PUT. Workflow opts into Node 24 ahead of the 2026-06-16 forced default.

## [2026-06-12] correction | Martin authors via WriterHelper (sibling Python/Qt app), not a Claude skill. The `new-post` **skill** I added was wrong → DELETED. The `new-post.mjs` **script** is fine and KEPT (a deterministic helper WriterHelper can call). Wrote the format-migration plan at `WriterHelper/PLAN_ASTRO_FORMAT.md`; added [[writerhelper]]. Future direction (deferred): fold WriterHelper into this site as a web app (it has a started `web/`+`webapi.py`). Also: I put `.claude/launch.json` in the Interverti repo by mistake (session cwd) — Martin moved it to this repo's `.claude/`.

## [2026-06-12] ingest | Design overhaul (Martin: scaffold looked "dull", wants flashy): constellation canvas hero (facet-hued nodes+links, reduced-motion static frame, theme-aware palette), Fraunces/Inter variable fonts, gradient wordmark + hero name, per-facet door hues w/ icons + glow hovers (asterisk icon for "Tout" = the book's wildcard motif), aurora background wash, glass sticky header, gradient chips. Fixed UTC date-shift bug in formatDate (frontmatter dates are UTC midnight — format with timeZone:'UTC'). Verified: dark desktop screenshot, light/mobile via computed styles (preview_screenshot tool wedges on this site: it resets the tab to the server root, which 404s under the /martingamsby.com base).

## [2026-06-13] ingest | Phase 2 migration script (`tools/migrate-jekyll.mjs`) built + run. Converts both Jekyll repos → `src/content/blog/{fr,en}` in the WriterHelper Astro format: drops the `### **title**` heading, `excerpt_image`→`image:`, drops `categories`, keeps tags. Outcome **265 posts** = 131 twin pairs + 2 FR-only (`ce-qui-peut-tout-gacher`, `crypto`) + 1 EN-only (`hello-world`); build validates all (283 pages). **Discovery: 5 FR posts carry copy-paste-wrong `ref:` URLs** (point at a neighbouring post — `ce-qui-peut-tout-gacher`/`les-blessures-de-votre-cerveau` both → `your-brain-injuries`; `moins-de-classement` + `le-classement-sans-dessus-dessous` + `le-gros-avantage-de-lecole` all → `the-big-advantage-of-school`). So pairing trusts `ref:` ONLY when it agrees with the date, then unique-date, then loose-ref; dedupes EN twins. Facets are a keyword first-pass needing manual review (report flags every non-`ideas`). Added schema `image:`. NOT yet done: redirect stubs in the two old repos (touches siblings); facet review; not committed. Updated [[jekyll-blogs]], PLAN.md.

## [2026-06-13] ingest | Image polish before commit. (1) **SEO filenames** — `localize-images.mjs` now names derivatives after the post slug (`/assets/posts/2026-06-11-11-years.{header,thumb}.webp`) instead of a sha1 hash (Martin: the original vercel-blob/wp names were junk anyway, so no point preserving them). Rewrote the tool: name = md basename; already-localized posts under an old name are renamed by **copying the existing webp** (no re-download); twins with the same basename (e.g. `2024-10-01-amazon` in both langs) share one file, differently-named twins get their own; the no-arg backfill **prunes** any `.webp` no post references. Re-ran: 113 updated, 106 old hash files pruned, 222 files (111 pairs), idempotent on 2nd run, build green. (2) **Inline body images** — the 2 dark-energy twin posts embedded dead `http://martingamsby.com/martin-gamsby/wp-content/uploads/2023/11/*.png` charts; Martin had the originals in `C:\Users\Martin\Downloads`. Copied 4 (chart-ESBH-Dark-energy15G, chart-ESBH-Dark-Energy-5G, UsedFor1DEstimation, Results1D — all tiny 12–16KB PNGs, kept as PNG so chart text stays crisp) into `public/assets/posts/`, rewrote the body `src`/`href` to `/assets/posts/<name>` (EN's WordPress `Results1D-300x131.png` thumbnail → the full `Results1D.png`). Verified on preview: all 4 inline charts load (0 broken), hero is the slug webp. The FR twin has no Results1D graph (wasn't in its migrated body — left as-is). Non-image WordPress "Source" links left untouched. Updated CLAUDE.md, [[writerhelper]].

## [2026-06-13] ingest | Self-hosted post images via `tools/localize-images.mjs` (new). Martin: stop hot-linking; download every external preview image and emit TWO sizes so the post list doesn't pull a full image for a 56px thumbnail. Script (Node 24, `sharp` 0.34.5 — promoted from transitive astro dep to an explicit `dependencies` entry) scans `src/content/blog/{fr,en}` frontmatter `image:`, downloads (http/https) or re-encodes (local `assets/...`) into `public/assets/posts/<sha1(source)>.header.webp` (≤1200px wide, q80) + `.thumb.webp` (160×160 cover, q72), then rewrites frontmatter to local `image:`+`imageThumb:` editing ONLY the image lines (body byte-for-byte intact). Idempotent (skips `/assets/posts/` images unless `--force`), dedupes twins by source. Modes: no-arg backfill, `--file <md>` (the **WriterHelper hook** — must run after writing any post with an `image:`), `--dry-run`. Schema gained `imageThumb` (optional); `PostList` now uses `imageThumb ?? image`, post hero uses `image`. Backfill run: **113 posts → 53 unique sources, 0 failed, 5.6MB** for 106 files (thumb ~5KB vs header ~66KB). Verified on preview: all 56 EN list thumbs serve `*.thumb.webp` (160px natural→56px), 0 broken; build green (275 pages). Added `npm run localize-images`. Updated CLAUDE.md, [[writerhelper]]. Nothing committed.

## [2026-06-13] ingest | Domain cutover + post images. Martin pointed `martingamsby.com` DNS at GitHub Pages and set the custom domain in repo settings; flipped `astro.config.mjs` to `site: https://martingamsby.com` + `base: '/'` (Martin had already done base) and added `public/CNAME` (`martingamsby.com`) so the Actions deploy doesn't drop the domain on each run. HTTPS may lag while GitHub provisions the cert → enable "Enforce HTTPS" once available. **Loose end:** the two old Jekyll repos' redirect stubs still point at the old `…/martingamsby.com/{lang}/blog/<stem>/` project URL — re-run `make-redirect-stubs.mjs --target-base https://martingamsby.com` there. **Images:** the `image:` frontmatter (107 external URLs from WriterHelper/xkcd/bsky + 6 local `assets/images/...` Jekyll refs) was migrated but the *local asset files were never copied* and were never rendered. Copied the 4 referenced local assets (`IntervertiProof.jpg`, `Interverti.jpg`, `Interverted.png`, `default-image.jpeg`) from the Jekyll repos into `public/assets/images/`; added `imageUrl()` to `i18n/ui.ts` (external URLs pass through, bare paths route through base); render a 56px square thumbnail in `PostList.astro` (right-aligned, rows w/o image keep text layout) and a capped hero (`.post-hero`, max-height 22rem) above the post body in `[...slug].astro`. Build OK (275 pages). Verified via preview_eval (screenshot tool still wedges per [[overview]] note): FR 11-ans hero loads (779×1039, IntervertiProof), blog list 56–57 thumbnails, 0 broken. **Still broken (not fixed):** the dark-energy twin posts embed dead `http://martingamsby.com/martin-gamsby/wp-content/...` WordPress image URLs in their bodies — now that the apex points at the new site these 404 (were already dead). Nothing committed/pushed. Updated CLAUDE.md, PLAN.md.

## [2026-06-13] ingest | Built the real `/book` page (was a `DoorStub`), since Interverti went into **pre-release on Amazon 2026-06-12** (FR edition first). `src/pages/[lang]/book.astro` now: hero with optimized cover (`<Image>` from `src/assets/book/interverti-{fr,en}.png`, copied from the Interverti repo's `content/{fr,en}/cover.png` and **downscaled to 900px** to keep the repo lean — build emits ~16–108KB webp variants), gradient pre-release badge w/ date, title/subtitle/byline, pitch, Amazon CTA, epigraph (Djosh Sho), the KDP back-cover blurb ("Imaginez un monde…"), FR-only early-reader pull-quotes, contact block, and the fiction-facet `PostList` (lens principle). EN page = "coming soon" variant (no ISBN yet) that points at the FR Amazon listing. **Resolved open question #6** (contact): publish `martingamsby@gmail.com` **obfuscated** — display `martingamsby [à] gmail [point] com`, parts in `data-*` on `.email-link`, inline script reassembles the `mailto:` in-browser; verified the served HTML has no verbatim address (`rawHasEmail:false`) yet the live link is `mailto:martingamsby@gmail.com`. Amazon link is a title-search placeholder (`amazon.ca/s?k=Interverti+Martin+Gamsby`) — swap for the direct ASIN/`/dp/` URL once known. Door blurbs updated "à paraître"/"coming soon"→pre-release. Build green (275 pages); FR/EN + mobile verified. Updated PLAN.md (#6) + [[profiles-and-channels]]. Nothing committed.

## [2026-06-13] ingest | Phase 2 committed in 4 commits across 3 repos + redirect stubs shipped. site repo: `71eea2e` migration (265 posts), `1ed8fb0` facet-map refinement (Interverti book universe→fiction, dark flow/hawking→physics; fiction 51→55) + new `tools/make-redirect-stubs.mjs`. Old repos turned into plugin-free redirect shells (new `_layouts/redirect.html` + per-post `layout: redirect` + `redirect_to:` → `…/martingamsby.com/{lang}/blog/<stem>/`): FR `martingamsby.github.io@3b9bdfe` (125 posts), EN `en@86c6e42` (124). **Gotcha (fixed): `git add _posts` and the stub script both swept Martin's UNTRACKED draft posts** (crypto, lutin/elf-day-2/3, 11-ans/11-years, killer-feature, i-need-help…) — overwrote them as stubs, and a first FR commit even tracked the empty `2026-06-11-.md`. Reset that FR commit, restored the drafts (inverse transform: layout→post, drop redirect_to), made the stub tool **git-aware** (only redirects tracked/published posts). Rule: redirect shells cover only what was actually PUBLISHED; untracked drafts migrate to the new site but stay untracked in the old repos. Side effect: those drafts are now live on the new site — mark `draft: true` if unwanted. Nothing pushed.

## [2026-06-13] ingest | Hooked WriterHelper to auto-localize post images (the user's "with every image add in WriterHelper too" ask). Context: a PARALLEL session had already built + run the site-side `tools/localize-images.mjs` (and kept revising it — hash-naming → slug-naming, 226 derivatives) while I built `/book`; I verified that side rather than duplicating, then did the genuinely-missing piece in the sibling `WriterHelper` repo. New `WriterHelper/localize.py` bridge: `set_excerpt_img()` now runs `node tools/localize-images.mjs --file <post.md>` ONLY for remote URLs (http/https/protocol-relative/data:; local paths skipped → tests stay offline), finds the site checkout by walking up to `tools/localize-images.mjs`, best-effort (logs + keeps remote URL if node/net down), then reads back `image:`/`imageThumb:` onto the model. Model gained `image_thumb`; `serializers.py` emits + round-trips `imageThumb` (load: astro reads it, jekyll sets ""), so editing a localized post never strips the thumb. Bridge is naming-scheme-agnostic (reads whatever the tool wrote) — survives the parallel session's ongoing tool churn. Tests: 28 pass incl. 3 new (remote localizes via monkeypatched bridge, local skips hook, imageThumb round-trips); real e2e smoke confirmed (temp post → node hook → `image:/assets/posts/…header.webp`+thumb, junk cleaned). Updated WriterHelper memory (`file-storage/astro-format.md`, `memory-map.md`) + this repo's [[writerhelper]]. Also: set `/book` Amazon link to the real `amazon.ca/dp/B0H53PGGBR`. Nothing committed. NB concurrent edits to this repo (localize tool/assets/posts, content frontmatter) are the parallel session's, not mine.

## [2026-06-14] ingest | Added brand icons to every external/social link from one royalty-free pack (Font Awesome Free, CC BY 4.0; build-time only, inlined as zero-JS SVG). Chose it over Simple Icons because SI no longer ships LinkedIn/Amazon. New: src/lib/icons.ts registry + src/components/Icon.astro; wired into footer, /links, DoorStubs (dev/physics/music), /book Amazon CTA. See [[link-icons]].

## [2026-06-16] ingest | Home constellation now hangs one representative post per gate as a "star" (Martin's ask). Decision: **manual popularity JSON is source of truth, refreshed on-demand by a script — not per-commit, not cron.** New `src/data/post-popularity.json` (translationKey→{score,...}, `_`-keys ignored), `src/lib/featured.ts` (`getGateFeatures`: per-gate sort by score→has-image→date, de-duped across gates, `GATE_FACET` maps book→fiction & everything→all), and `tools/fetch-popularity.mjs` (`npm run fetch-popularity`) which updates only entries with a `links` map: Bluesky likes+reposts (public API, no auth — verified live against martingamsby.bsky.social) and YouTube views+likes (needs `YOUTUBE_API_KEY`); `score = manual + Σ(sources×WEIGHTS)` unless numeric `pin`; Typeshare/FB/X have no free API → hand-score. Render: feature chips are `.sky-social.sky-post` inside the floating `.sky-socials` list, so [[Constellation.astro]] needs ZERO script changes — they just carry `data-facet`(=gate)+`data-weight`; ~2× a social chip, circular `imageThumb`, eager-loaded, ★-glyph fallback when imageless. Verified on preview: 5 stars, correct facets/hrefs/thumbs (HTTP 200), seeded `burnout` score=8 wins the `everything` gate (proves popularity path + glyph fallback). Build green (279 pages). New page [[featured-stars]]; updated CLAUDE.md. Nothing committed.

## [2026-06-16] correction | `npm run fetch-popularity` "did nothing" — by design it only updated entries with a `links` map, and the JSON had none, so it sat there. Fixed the UX: rewrote `tools/fetch-popularity.mjs` with `--seed` (scaffold `{score:0,title}` for every post → a fill-in db; reads frontmatter off disk, de-dupes twins, idempotent) and `--list` (print each gate's candidates + current ★). Ran `--seed`: **131 entries written.** All score 0 → site behaviour unchanged (fallback = most-recent-with-image), build still 279 pages. **Empirical finding (recorded in [[featured-stars]]):** measured both Bluesky author feeds — ~0 likes/reposts and the linked posts point at YouTube not the blog, and the blog has no analytics. So auto-fetch is marginal; manual scoring is the real path, YouTube views the only worthwhile auto-source (per-post `links.youtube`). Don't build a Bluesky→post crawler. Martin tweaked post-star weight 1.9→1.3 in index.astro + added a "remove if you don't want articles" comment. Updated CLAUDE.md. Nothing committed.

## [2026-06-16] ingest | Made `npm run fetch-popularity` actually automatic (Martin ran the bare command twice, "it does nothing"). **Earlier reality-check was too pessimistic** — corrected: YouTube view counts ARE harvestable with NO API key. The no-flag run now: (1) seeds missing entries; (2) discovers video IDs from 3 channels' `/videos` pages (`@MartinGamsby` 30, `@MartinGamsbyEN` 9, music `UCX6Ksv…` 30) **plus** both Bluesky feeds' external links (reaches older videos the ~30-cap page omits → 79 total); (3) scrapes each watch page for `"viewCount":"…"`; (4) matches video title→post title (normalized exact/prefix, ≥15-char guard — music covers like "Take On Me" correctly don't match); (5) writes `sources.ytView`(+`links.youtube`), `score = manual+Σ(sources×WEIGHTS)`. First run: 79 scraped, **9 matched** (multi-video posts sum: e.g. just-a-few-hours 44+518=562, microphone 47+151=198). New stars: dev→microphone(198), music→just-a-few-hours(562), everything→piano(86); book/physics stay on fallback (no companion video). Bluesky engagement still ~0 → used only for discovery, not scoring; YouTube views are the sole real auto-signal; rest hand-scored; `manual`/`pin`/`score` survive re-runs. Channel/handle lists hardcoded in the tool (mirror src/data/socials.ts). Build green (279 pages). Updated [[featured-stars]] + CLAUDE.md. Nothing committed.

## [2026-06-17] ingest | Scoped the popularity sources after Martin's questions. (1) **Dropped the "Music YouTube" channel** (`UCX6KsvwOo2U2zrBumOL2bww`) from the fetch tool's CHANNELS — it's pure covers/tutorials with no blog companions (Martin: "don't check it, there's no posts from this account yet"). Re-run confirmed identical 9 matches with 49 videos instead of 79 (faster, no matches lost). (2) **Bluesky has NO view/impression metric** — dumped a post's fields: only `likeCount/repostCount/replyCount/quoteCount/bookmarkCount`, all 0. So Bluesky stays a discovery-index only. **FR feed (`martin-gamsby`) is scanned and matters most**: it yields 8 videos vs EN's 6, and its FR-titled uploads match FR post titles (accent-insensitive norm) → same translationKey → views sum with EN (e.g. just-a-few-hours 518 FR + 44 EN = 562). (3) **Probed X/Facebook/Typeshare** to see if the YouTube scrape generalizes — it doesn't: X is a JS shell (authed GraphQL only), FB is a login wall, Typeshare is a client-rendered SPA with no data in HTML / no public API. So YouTube view count remains the sole auto-signal; rest hand-scored. Updated [[featured-stars]] + CLAUDE.md. Nothing committed.

## [2026-06-17] ingest | Retired guidepour.com into martingamsby.com + Djosh Sho author page. guidepour.com is Martin's dead Grav site (latest `C:\wamp64-3.2.3\www\guidepour.com2`) — the in-universe "Guide pour ✳" column by the fictional Djosh Sho (narrator of Interverti). **Key discovery: the 16 live entries already exist on the site** as `fiction` posts in BOTH FR+EN (converted to Gamsblurb by Grav's `convert.py`, then migrated) — so this was redirects + author page + tag-tidy, not a content import. **Second discovery: the old Grav routes are FRENCH and rarely match the filename** (`/metropole`→grand-metropolien, `/maths`→mathematicien, `/jeune`→jeune-intermittent, `/dejavu`→deja-vu) → redirect targets can't be derived, must be stored. Built: (1) new optional `aliases[]` schema field; backfilled all 32 posts (16 FR = the immutable book slug; 16 EN = EN post slug, best-guess for the future guidance4.com — `web5`/`web-5-0` split on purpose to avoid a collision) + normalized tags (added `Fiction` where missing) via a one-off script. (2) `src/lib/guidepour-redirects.mjs` Astro integration (`astro:build:done`) emits `dist/<alias>/index.html` meta-refresh stubs → `/{lang}/blog/<stem>/`, **fails the build on duplicate/colliding alias** (CI runs `withastro/action`=`astro build` directly, so a postbuild npm script would be skipped — hence an integration). (3) `/{lang}/guidepour` page: Djosh Sho bio (ported from Grav `author.fr.md`, EN translated) **with an explicit fiction note** (Martin's ask), Coal Ton mention, entry list via new `isGuidePour`/`getGuidePourPosts` (filter on the cross-lang `Djosh Sho` tag), linked ↔ /book + nav + friendlier 404. **Redirect layer 2 = Martin's registrar** (path-forward `guidepour.com/* → martingamsby.com/*`, bare slug since forwarding can't add a prefix; GitHub Pages repo as fallback). Verified on preview: 32 stubs serve 200 w/ correct FR/EN targets, `/agnostique` meta-refresh lands on the post, unknown slug 404s, both guidepour pages render (16 entries, bio, fiction note, book link). Build green (281 pages). **Gaps flagged to Martin (in the book):** missing entry #06, hidden `/crypto` article (never on site), full printed-slug list, guidance4 slugs, Coal Ton bio. New page [[guidepour-redirects]]; resolved PLAN open-Q#4; updated CLAUDE.md. Nothing committed.

## [2026-06-17] ingest | Found + fixed a real coverage bug in fetch-popularity, added a YouTube Data API path. (1) **Bug:** the channel `/videos` page HTML only carries ~30 uploads; `@MartinGamsby` actually has **59** — so the no-key scrape was silently dropping ~29 (only partly backfilled via Bluesky). (2) Tried the no-key innertube continuation to paginate the rest: it's **flaky/nondeterministic** (30 one run, 59 another, 0 a third) — the newer grid uses `lockupViewModel`/`contentId` not `videoId`, and tokens sometimes return non-video shelves. Also, scraping ~50 watch pages **rate-limits to HTTP 429** (hit it from this session's debugging — that's why a run showed "0 scraped"/consent page). (3) **Fix:** added a proper **YouTube Data API mode** gated on `YOUTUBE_API_KEY` — `channels`→uploads playlist, `playlistItems` paginates every upload, `videos.list` batches views+likes 50/call (≈2 requests for 60 videos, complete, no rate-limit). No-key path kept as best-effort (channel page + Bluesky + scrape) with a warning to set a key. **API path written but NOT yet run with a real key — needs verification.** UA header now sent (YouTube serves a lighter page without one). (4) **Facebook Q:** a Page Graph token reads only that Page (own posts + insights incl. impressions), not arbitrary/profile content; blog is on the personal profile, so the token helps only if blog content is also posted on the business Page. Existing scores preserved (verified via `--offline`: stars stay microphone 198 / just-a-few-hours 562 / piano 86). Updated [[featured-stars]] + CLAUDE.md. Nothing committed.

## [2026-06-17] ingest | Put the tag vocabulary to work in two constellations (authored/cleaned in [[writerhelper]]'s new tag system). (1) **Per-post related-posts map** — `src/lib/related.ts` ranks other posts by shared tags (house tag ignored) → shared facets → `scoreOf` → recency; `[...slug].astro` renders them via a new shared `StarMap.astro` under the body. (2) **Tag galaxy** `/[lang]/tags` — `src/lib/tags.ts` derives per-tag frequency + dominant facet from post data (no config); the page renders a star field (size=frequency, colour=facet, count in chip) + a click-to-filter PostList (new `tagData` flag → `data-tags`). (3) **`StarMap.astro`** — focused, self-contained constellation reusing the home sky's `.sky-social`/`.sky-post` chips + `--c-<gate>` palette but decoupled from `Constellation.astro`; rings + canvas connector lines + gentle float, no-JS fallback, reduced-motion/visibility/theme aware. Added `gateOf`/`FACET_GATE` to `src/lib/blog.ts`, a **Tags** nav entry, and i18n keys (fr+en). Build green (285 pages). Verified on the built dist: galaxy = 44 stars w/ lines drawn, Fiction filter → 23 posts; related map renders relevant posts per article. New pages [[related-constellation]], [[tag-galaxy]]; cross-linked [[featured-stars]]. Nothing committed.

## [2026-06-17] fix | Guidepour ≠ always fiction + two constellation polish items. (1) `getGuidePourPosts` now lists by the `Djosh Sho` tag ONLY (dropped the `getPosts(lang,'fiction')` pre-filter) — some "Guide pour" entries are real-life facts, not fiction, and must still appear; WriterHelper's `migrate_tags.py` now drops the `Fiction` *tag* on a Djosh Sho post that lacks the `fiction` *facet*, so the facet is the single lever. (2) Home page hangs a `✳` star on the **book gate** linking to `/[lang]/guidepour/` (a `.sky-post` chip, `data-facet="book"`, in index.astro). (3) `StarMap.astro` `coreEdge` now honours the core pill's rounded ends (arc intersection like `Constellation.astro`'s `rectEdge`) — fixes connector lines stopping ~5px short in the transparent rounded-corner gap; verified endpoints land exactly on the pill border. Build green (285 pages). Updated [[guidepour-redirects]]. Nothing committed.

## [2026-06-17] fix | Constellation DRY + guidepour de-fiction + list label. (1) **Shared geometry:** extracted `rectEdge`/`circEdge`/`orbitPoint` into `src/lib/constellation-geometry.ts`, now imported by BOTH `Constellation.astro` (home) and `StarMap.astro` (related/galaxy) — previously the rounded-corner edge maths was duplicated (Martin: "why aren't you reusing the same code?"). The pill line-attachment fix lives there once; verified line endpoints land exactly on the pill border (vs ~5px gap with the old sharp-rect edge). (2) **De-fiction:** the mistaken `Fiction` *tag* was added in `d86cdbf` (NOT 3876900, which is an IG image); removed it from the 8 real-life-fact pairs (those that lacked it pre-`d86cdbf`). `facets:[fiction]` kept (Martin's call — still Djosh Sho/Book pieces). WriterHelper's migrate now never pair-unions Fiction across a guidepour twin. The 8 still list on `/guidepour` (keyed off the Djosh Sho tag). (3) `/guidepour` entries now read "Guide pour *title*" / "Guidance for *title*" via `PostList`'s new `titlePrefix`. Build green (285 pages). Updated [[guidepour-redirects]], [[related-constellation]]. Nothing committed.

## [2026-06-17] fix | Two constellation/label refinements. (1) `/guidepour` list label uses LITERAL asterisks — "Guide pour *title*" / "Guidance for *title*" (PostList renders `*{title}*` as text, not `<em>`; Martin: "with a star, not italized"). (2) The related-constellation core node now shows the FULL post title — wraps to multiple lines (was nowrap + ellipsis, which cropped long titles like the "Flot sombre" post); `StarMap` reads the core's computed `border-radius` so connector lines still land on the border (verified max error 0). Build green (285 pages). Updated [[related-constellation]]. Nothing committed.

## [2026-06-18] correction | EN guidance terminology + real book slugs, from the source of truth (Martin: "in english it's not 'Guide for', it's 'Guidance For'"). Source: `Interverti/content/interverti/en/book.draft.md` (EN book) — uses "**Guidance for ✳**" and prints `guidance4.com/<slug>`. (1) Renamed EN "Guide For/for" → "Guidance For/for" across the 16 EN posts (tag + body footer), the `/guidepour` EN strings (title/lede/fiction-note/heading), nav, `/book` link, `404`; the EN bio is now the book's **exact** ch.10 text (was my translation). (2) Updated the 9 EN aliases to the book's real slugs: grand-metropolitan→**metropolis**, intermittent-fasting→**fasting**, sleeper→**sleep**, web-5-0→**web5**, conference→**conferences**, hypocritical→**hypocrisy**, hasty-generalization→**generalization**, mathematician→**math**, fabulator→**fabulist** (7 unchanged). (3) **Collision found:** the EN book's `web5` and `conferences` are IDENTICAL to the French slugs → guidepour.com + guidance4.com can't share one martingamsby.com bare-slug namespace. Scoped the integration to **`EMIT_LANGS=['fr']`** (guidepour.com only, 16 FR stubs); guidance4.com gets its own redirect layer later (EN `aliases` are the recorded data). (4) **Gaps surfaced:** the EN book has **33** guidance entries, only 16 have posts → **17 new** (bicycle, bunker, backward-compatibility, end-of-the-world, adrenaline, cows, old, music, flaws, tesla, wisdom, mozart, everyone, einstein, resilience, newton, slaves) — texts are in the book. Also EN post titles differ from book entry titles (Sleeper vs Sleep, Mathematician vs Regression, Fabulator vs Fabulist…) — flagged, not auto-retitled. Left Fiction tags as the parallel session set them; did NOT touch PostList/titlePrefix. Build green (285 pages, 16 FR stubs, /en/guidepour shows 0 "Guide for"). Updated [[guidepour-redirects]], CLAUDE.md. Nothing committed.

## [2026-06-18] correction | Per Martin's follow-up: emit EN redirects too + retitle to the book. (1) Integration now `EMIT_LANGS=['fr','en']` (was `['fr']`) — guidepour.com AND guidance4.com both forward bare `/<slug>` → martingamsby.com/<slug>, so both emit. Guard reworked: a SAME-language duplicate still throws (real bug); a CROSS-language collision (`web5`, `conferences` — printed identically in both books) is expected → FR-first wins, EN stub skipped with a build WARNING, so guidance4.com/web5 lands on the FR post (toggle = escape). Last build: **30 stubs** (16 FR + 14 EN-only), 2 warnings, green. (2) Retitled 5 EN posts to the book's entry titles (Martin: "retitle to match book"): Sleeper→Sleep, Conference→Conferences, Hypocritical→Hypocrite, Mathematician→Regression, Fabulator→Fabulist (verified H1s in dist; FR twin titles left — need FR book). (3) Investigated Martin's "they should have posts already": the **17 extra EN book entries have NO matching posts** (searched distinctive content — seed-vault/bunker, bicycle-energy, Tesla/Newton/Mozart, etc.; none exist). They're genuine new-to-2026-book gaps, not mis-slugged posts. Verified EN stubs land correct (metropolis→grand-metropolitan, sleep→sleeper, math→mathematician, fabulist→fabulator…). Updated [[guidepour-redirects]], CLAUDE.md, PLAN.md. Nothing committed.

## [2026-06-18] ingest | Generated the 17 missing Guide Pour entries from BOTH books + synced existing to book text (Martin pushed back: "17 no matching posts? I don't believe you" → verified against both books; he was right to check, but the 17 are genuinely new to the 2026 book). Got the FR book path: `Interverti/content/interverti/fr/book.md` (immutable, published) + EN `.../en/book.draft.md` (draft, MUTABLE → can change EN to dodge collisions). Both books = same 33 entries, same order. Built a generator (`_tmp_guidepour_gen.mjs`, parses the `## Guide pour \*X\*` / `## Guidance for \*X\*` blocks → title/slug/body, strips bold+escapes): **wrote 17 new FR + 17 new EN posts** (facets:[fiction], Djosh Sho tag, book-slug alias, footer); dated in **book order** (6 before Magie at in-between dates 2016-03-15…2017-04-20, 11 after at daily 2017-05-02…05-12; fake dates, real unknown). **Retitled** existing to book (FR Dormeur→Sommeil, Conférencier→Conférences, Mathématicien→Régression; EN done prior) and **fixed 2 wrong FR aliases vs the immutable FR book** (dormeur→sommeil, dejavu→deja-vu). A 2nd script synced all 16 existing bodies to current book text (books were edited since the .md were made — e.g. agnostique "pas assez d'évidences"→"pas de preuves"), preserving footers/social-links. **Collisions are now 9** (web5, conferences, deja-vu, adrenaline, tesla, mozart, einstein, resilience, newton — proper nouns/cognates identical in both books); EMIT_LANGS=['fr','en'] FR-wins → **57 stubs** (33 FR + 24 EN-only). Build green, **319 pages**, /fr+/en/guidepour list 33 each, new posts + redirects verified (bicyclette→FR, bicycle→EN, bunker→EN, sommeil→dormeur post). NB: a transient `Duplicate id` glob-loader warning appears when the parallel session's dev server races `.astro` mid-build — benign (same file, "later wins"), absent in CI and on clean re-run. **Open: guidance4.com hosting** — recommend its own repo (keeps /tesla etc. correct-language) vs renaming 9 EN slugs. Updated [[guidepour-redirects]], PLAN.md, CLAUDE.md. Nothing committed.

## [2026-06-18] correction | Resolved the 9 FR/EN slug collisions by renaming the EN side (Martin: "I told you to rename the EN ones. the en book is NOT printed."). The EN book is a draft → mutable, so renamed the 9 overlapping EN slugs in BOTH the EN posts' `aliases:` and `Interverti/content/interverti/en/book.draft.md`: web5→web-5-0, conferences→conference, deja-vu→dejavu, adrenaline→adrenalin, tesla→nikola-tesla (full names for the 4 proper nouns: +wolfgang-mozart, albert-einstein, isaac-newton; resilience→resiliency). Reverted the integration guard from "FR-wins-on-cross-lang-collision" back to **throw on ANY duplicate** (no silent collisions). Build green: **66 stubs** (33 FR + 33 EN, all unique), 0 collisions; verified each EN slug lands on its EN post and each FR slug on its FR post (nikola-tesla→/en, tesla→/fr; web-5-0→/en, web5→/fr; conference→/en, conferences→/fr). The transient `Duplicate id` glob race (parallel dev server) still cosmetically appears mid-build; CI-clean. guidance4.com can now share the martingamsby.com bare namespace (both forward `/<slug>`→martingamsby.com/<slug>). Updated [[guidepour-redirects]] (+a rename table), CLAUDE.md, PLAN.md. Nothing committed.

## [2026-06-18] correction | Fixed a paragraph-flattening regression I caused (Martin: "why did you remove the paragraphs ... wtf"). The earlier "sync existing bodies to the book" replaced each body with the book's single dense bold-block → lost the blog's paragraph breaks. Fix in two passes (both skip anything already multi-paragraph, so concurrent hand-fixes are respected): (1) **reliable restore** — for posts whose text barely changed, re-applied git-HEAD's paragraph structure to the updated text by matching paragraph-start prefixes accent/hyphen-insensitively (agnostique 5 paras, poisson-rouge 3, conferencier/conference 2 — book wording kept, breaks back). (2) **heuristic split** — for the book-rewritten dormeur/sleeper + the 17 new posts (no original paragraphing exists), split single blocks at sentence boundaries (~2 sentences/para, ≤2-sentence entries left whole). Verified: all **66** guidepour posts paragraphed, 0 walls (>450-char single blocks); build green 319 pages. The new/rewritten ones are best-effort auto-splits — Martin to tweak break positions to taste. Lesson: never flatten paragraphs when syncing prose to a single-block source. Nothing committed.
