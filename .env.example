# Copy this file to `.env` (which is gitignored) and paste your real key.
# Used by `npm run fetch-popularity` for complete, reliable YouTube stats.
# Get one: Google Cloud console → APIs & Services → enable "YouTube Data API v3"
#          → Credentials → Create credentials → API key.
YOUTUBE_API_KEY=
